extends layout
block content
  .intro-med
    .container
      .row.titular
        .col-sm-12
          h1 ALIMENTOS Y BEBIDAS.
      .row
      -for(var i=0; i<alimentos.length; ++i){
        .col-sm-6.item-med
          h4=alimentos[i].nombre
          img(src="img/#{alimentos[i].imagen}", width="200")
          p="Descripción: " + alimentos[i].descripcion
          strong="$"+alimentos[i].precio
          br
          if(isAuthenticated)
            button.cart.producto(precio=alimentos[i].precio titulo=alimentos[i].nombre)="Comprar"
          if(!isAuthenticated)
            form(action="http://localhost:8080/signin")
              button.cart="Comprar"
      -}
